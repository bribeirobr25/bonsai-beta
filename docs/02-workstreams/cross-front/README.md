# Cross-front

Material that belongs to no single front, plus our validation of what they sent.

## Reconciliation has run — 4 September 2026

**`CROSS-FRONT-RECONCILIATION.md`** is the output. Both amendments adopted, gate
taxonomy and validation sequence confirmed, all six growth mechanisms still
eligible, no contradiction found that kills a loop.

It also records what the founder's `S1` decision changed — the fronts all closed
before it was made.

## Start here to understand the alignment

**`CROSS-FRONT-MAPPING.md`** — an **11-row matrix** aligning every W2 gate to its
W3 territory and W4 loop, each row carrying the joint unresolved question and a
disposition. It is the one-page view of where the three fronts agree and what is
still open.

*Provenance: W2 filed this in their own archive and referenced it from nothing.
Nothing supersedes it; the prose summary that replaced it carries less. Promoted
here 4 Sep 2026.*

## The rest

| File | What it is |
|---|---|
| `W2-DETAILED-RESPONSES-TO-W3-W4.md` | W2's substantive answers to W3's seven questions and W4's eight. Marked superseded by W2, but **99% of its content exists nowhere else** — the alignment summary that replaced it is a summary. |
| `W4-RESPONSE-TO-W3.md` | W4's formal acceptance of W3's guardrails and Brand kill condition |
| `W2-W3-W4-PACKAGE-VALIDATION.md` | our round-1 validation — 30 adoption items, both amendments |
| `W2-W3-W4-ROUND2-VALIDATION.md` | our round-2 validation — measured coverage, the two persisting gaps, the tiering defect |

## State

W1 `CLOSED` · W2, W3, W4 all returned and **reconciled** · `S1` authoritative ·
**`Step 0` run** (`MIXED · DENSITY RISK HIGH` — see
`04-evidence/W4-L4A-CORPUS-DENSITY-SHADOW-TEST.md`).

Everything after Step 0 needs **participant access**, which is now the binding
constraint on the project.

The joint gate all three converged on:

> **Can a dedicated experience around one real tree create current differentiated
> value over Photos / Excel / notes, before collection-wide migration or long
> historical accumulation?**

W2 `G1+G2` = W3 `Progress → Memory` = W4 `L2`.

Canon §11a holds the aligned state, §11b the adopted frameworks, §11c the founder
decision on success ambition.
