# Provenance · Bonsai_W1_FINAL_CLOSED_2026-09-03

**Received:** 3 September 2026, via `docs/00-inbox/` as
`Bonsai_W1_FINAL_CLOSED_2026-09-03.zip`.

**SHA-256 of the received zip, recorded at receipt before extraction:**

```
d1721920b7cff847cef09434011eff99bd6205946a6822559bab37a9bebf8c6d
```

**The original zip is no longer on disk.** It was never committed to git, and
what removed it could not be determined. This is recorded rather than glossed
over because the zip was the outermost integrity envelope.

**No content was lost.** The extracted tree is complete (25 files plus the
sender's `SHA256SUMS.txt`) and all **25 of 25** files verify against the
sender's own manifest, which is the integrity check that actually matters —
it is independent of the zip and signed by the sender's own hashes.

Re-verify at any time:

```sh
cd Bonsai_W1_FINAL_CLOSED_2026-09-03 && shasum -a 256 -c SHA256SUMS.txt
```

**Contents are unmodified.** Locally amended working copies live in
`01-canon/`, `02-workstreams/` and `03-registry/`, each carrying a provenance
banner naming what changed. This directory is the reference original.
